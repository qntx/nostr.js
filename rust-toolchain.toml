[toolchain]
# Pin to the workspace MSRV (1.94, edition 2024). Bumping this
# requires updating rust-version in crates/nostr-crypto-wasm/Cargo.toml.
channel = "1.94"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
profile = "minimal"
