node_modules
dist
*.tgz
*.log
.DS_Store
!.vscode/
!.vscode/settings.json
!.vscode/extensions.json

# dotenv environment variable files
.env
.env.*
!.env.example

# Local reference checkouts (not published / not required for build)
3rdparty

# Internal documentation (kept locally, not published)
docs/*
!docs/index.mdx
!docs/meta.json

# Rust / wasm artifacts (built by scripts/build-wasm.sh, not committed)
/target
src/wasm/generated/
src/wasm/nostr_crypto_wasm_bg.wasm

# Hermes smoke bundle (built by scripts/hermes-smoke.sh, not committed)
.hermes-smoke.iife.js
